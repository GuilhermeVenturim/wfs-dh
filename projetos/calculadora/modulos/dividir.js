export function dividir(numA, numB){
    let resultado = []
    if(numA != 0 && numB != 0){
        return resultado = numA / numB;
    }else{
        return 'Por favor, tente novamente com um número diferente de 0'
    }
}
//console.log(dividir(10, 0)) testado
//console.log(dividir(10, 2)) testado