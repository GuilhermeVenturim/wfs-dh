function multiplicar(numA, numB){
    let resultado = []
    if(numA != 0 && numB != 0){
        return resultado = numA * numB;
    }else{
        return 'Por favor, tente novamente com um número diferente de 0'
    }
}
//console.log(multiplicar(10, 0)) //testado
//console.log(multiplicar(10, 2)) //testado